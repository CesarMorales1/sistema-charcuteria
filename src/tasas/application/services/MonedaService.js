export class MonedaService {
  constructor(monedaRepository) {
    this.monedaRepository = monedaRepository;
  }

  async obtenerMonedas() {
  }

  async obtenerMonedaPorId(id) {
  }

  async obtenerMonedaPorCodigo(codigo) {
  }

  async obtenerMonedaPrincipal() {
  }
}
