export class MonedaController {
  constructor(monedaService) {
    this.monedaService = monedaService;
  }

  obtenerMonedas = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerMonedaPorId = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerMonedaPorCodigo = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };
}
