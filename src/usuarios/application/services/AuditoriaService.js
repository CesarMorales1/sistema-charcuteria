export class AuditoriaService {
  constructor(auditoriaRepository) {
    this.auditoriaRepository = auditoriaRepository;
  }

  async registrarAuditoria(auditoriaData) {
  }

  async obtenerAuditorias(filtros) {
  }

  async obtenerAuditoriaPorId(id) {
  }

  async obtenerAuditoriasPorUsuario(usuarioId, filtros) {
  }

  async obtenerAuditoriasPorTabla(tabla, idRegistro) {
  }
}
