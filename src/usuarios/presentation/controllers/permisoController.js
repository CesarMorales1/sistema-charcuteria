export class PermisoController {
  constructor(permisoService) {
    this.permisoService = permisoService;
  }

  obtenerPermisos = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerPermisoPorId = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerPermisosPorModulo = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerPermisosPorUsuario = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };
}
