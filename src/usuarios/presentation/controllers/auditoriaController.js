export class AuditoriaController {
  constructor(auditoriaService) {
    this.auditoriaService = auditoriaService;
  }

  obtenerAuditorias = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerAuditoriaPorId = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerAuditoriasPorUsuario = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerAuditoriasPorTabla = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };
}
