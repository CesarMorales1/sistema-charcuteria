export class InventarioController {
  constructor(inventarioService) {
    this.inventarioService = inventarioService;
  }

  obtenerInventario = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerInventarioProducto = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  registrarMovimiento = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerMovimientos = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };
}
