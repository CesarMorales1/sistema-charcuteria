export class PagoController {
  constructor(pagoService) {
    this.pagoService = pagoService;
  }

  registrarPago = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerPagos = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerPagoPorId = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  obtenerPagosPorFactura = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };

  calcularTotalPagado = async (req, res, next) => {
    try {
    } catch (error) {
      next(error);
    }
  };
}
